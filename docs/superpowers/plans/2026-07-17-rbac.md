# RBAC Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** RBAC с permissions: роль = контейнер прав, права выдаются ролям через API, guard проверяет `@RequirePermissions(...)` по данным из БД, ownership — в сервисе.

**Architecture:** Второй глобальный guard (`PermissionsGuard`) после `JwtAuthGuard` читает метаданные декоратора и грузит права пользователя из БД. Два feature-модуля (`permissions`, `roles`) управляют каталогом прав и их раздачей. Spec: `docs/superpowers/specs/2026-07-17-rbac-design.md`.

**Tech Stack:** NestJS 11, Prisma 7 (`prisma-client`, адаптер `@prisma/adapter-pg`, клиент в `src/generated/prisma`), PostgreSQL, Jest.

---

### Task 1: Схема Prisma — связь User ↔ Role, переименование Permission

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] **Step 1: Обновить схему**

Модель `Permissions` переименовать в `Permission` (единственное число — конвенция; таблица остаётся `permissions` благодаря `@@map`). К `User` добавить `role`/`roleId`, к `Role` — обратную связь `users`:

```prisma
model User {
  id  Int @id @default(autoincrement())
  email String @unique
  name  String? @default("")
  password String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  userSetting UserSetting?
  pets Pet[]
  role   Role? @relation(fields: [roleId], references: [id])
  roleId Int?

  @@map("users")
}

model Role {
  id Int @id @default(autoincrement())
  name String @unique
  permissions Permission[] @relation("role_permissions")
  users User[]

  @@map("roles")
}

model Permission {
  id Int @id @default(autoincrement())
  name String @unique
  roles Role[] @relation("role_permissions")

  @@map("permissions")
}
```

- [ ] **Step 2: Миграция и регенерация клиента**

```bash
npx prisma migrate dev --name user_role
```

Ожидаемо: миграция добавляет колонку `roleId` в `users` (переименование модели таблицы не трогает). Клиент перегенерируется автоматически; если нет — `npm run db:generate`.

- [ ] **Step 3: Проверить сборку**

```bash
npx tsc --noEmit
```

Ожидаемо: без ошибок (тип `Permissions` нигде в вашем коде пока не использовался).

- [ ] **Step 4: Commit**

```bash
git add prisma src/generated
git commit -m "User to role relation, rename Permission"
```

---

### Task 2: Сид базовых ролей и прав

**Files:**
- Create: `prisma/seed.ts`
- Modify: `prisma.config.ts`

- [ ] **Step 1: Написать `prisma/seed.ts`**

Идемпотентный (upsert по `name`) — можно гонять сколько угодно раз:

```ts
import 'dotenv/config';
import { PrismaClient } from '../src/generated/prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';

const adapter = new PrismaPg({
  connectionString: process.env.DATABASE_URL as string,
});
const prisma = new PrismaClient({ adapter });

const ALL_PERMISSIONS = [
  'pets:create',
  'pets:read',
  'pets:update',
  'pets:delete',
  'pets:manage-any',
  'users:read',
  'users:manage',
];

const USER_PERMISSIONS = ['pets:create', 'pets:read', 'pets:update', 'pets:delete'];

async function main() {
  for (const name of ALL_PERMISSIONS) {
    await prisma.permission.upsert({ where: { name }, update: {}, create: { name } });
  }

  await prisma.role.upsert({
    where: { name: 'admin' },
    update: { permissions: { set: ALL_PERMISSIONS.map((name) => ({ name })) } },
    create: { name: 'admin', permissions: { connect: ALL_PERMISSIONS.map((name) => ({ name })) } },
  });

  await prisma.role.upsert({
    where: { name: 'user' },
    update: { permissions: { set: USER_PERMISSIONS.map((name) => ({ name })) } },
    create: { name: 'user', permissions: { connect: USER_PERMISSIONS.map((name) => ({ name })) } },
  });

  console.log('Seed completed');
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(() => prisma.$disconnect());
```

- [ ] **Step 2: Подключить сид в `prisma.config.ts`**

В блок `migrations` добавить строку `seed`:

```ts
  migrations: {
    path: "prisma/migrations",
    seed: "ts-node prisma/seed.ts",
  },
```

- [ ] **Step 3: Прогнать сид и проверить**

```bash
npx prisma db seed
```

Ожидаемо: `Seed completed`. Проверка глазами: `npx prisma studio` → в `roles` две роли, у `admin` 7 прав, у `user` 4.

- [ ] **Step 4: Commit**

```bash
git add prisma/seed.ts prisma.config.ts
git commit -m "Seed roles and permissions"
```

---

### Task 3: Permissions-модуль (каталог прав)

**Files:**
- Modify: `src/modules/permissions/dto/create-permission.dto.ts`
- Delete: `src/modules/permissions/dto/update-permission.dto.ts`
- Modify: `src/modules/permissions/permissions.service.ts`
- Modify: `src/modules/permissions/permissions.controller.ts`
- Modify: `src/app.module.ts`

Update-эндпоинт удаляем (YAGNI: переименование права разорвёт связь с `@RequirePermissions` в коде — лучше запретить).

- [ ] **Step 1: DTO**

`create-permission.dto.ts`:

```ts
import { IsString, Matches } from 'class-validator';

export class CreatePermissionDto {
  @IsString()
  @Matches(/^[a-z]+:[a-z-]+$/, {
    message: 'Permission name must look like "resource:action"',
  })
  name: string;
}
```

Файл `update-permission.dto.ts` удалить.

- [ ] **Step 2: Сервис**

Паттерн ошибок — как в `users.service.ts`: P2002 → 409, P2025 → 404. `PrismaModule` глобальный, импортировать его в модуль не нужно.

```ts
import { ConflictException, Injectable, NotFoundException } from '@nestjs/common';
import { CreatePermissionDto } from './dto/create-permission.dto';
import { PrismaService } from '../../libs/database/prisma.service';
import { Prisma } from '../../generated/prisma/client';

@Injectable()
export class PermissionsService {
  constructor(private prisma: PrismaService) {}

  async create(createPermissionDto: CreatePermissionDto) {
    try {
      return await this.prisma.permission.create({ data: createPermissionDto });
    } catch (e) {
      if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2002') {
        throw new ConflictException('Permission with this name already exists');
      }
      throw e;
    }
  }

  findAll() {
    return this.prisma.permission.findMany();
  }

  async findOne(id: number) {
    const permission = await this.prisma.permission.findUnique({ where: { id } });
    if (!permission) throw new NotFoundException(`Permission with ID ${id} not found`);
    return permission;
  }

  async remove(id: number) {
    try {
      return await this.prisma.permission.delete({ where: { id } });
    } catch (e) {
      if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2025') {
        throw new NotFoundException(`Permission with ID ${id} not found`);
      }
      throw e;
    }
  }
}
```

- [ ] **Step 3: Контроллер**

```ts
import { Controller, Get, Post, Body, Param, Delete, ParseIntPipe } from '@nestjs/common';
import { PermissionsService } from './permissions.service';
import { CreatePermissionDto } from './dto/create-permission.dto';

@Controller('permissions')
export class PermissionsController {
  constructor(private readonly permissionsService: PermissionsService) {}

  @Post()
  create(@Body() createPermissionDto: CreatePermissionDto) {
    return this.permissionsService.create(createPermissionDto);
  }

  @Get()
  findAll() {
    return this.permissionsService.findAll();
  }

  @Get(':id')
  findOne(@Param('id', ParseIntPipe) id: number) {
    return this.permissionsService.findOne(id);
  }

  @Delete(':id')
  remove(@Param('id', ParseIntPipe) id: number) {
    return this.permissionsService.remove(id);
  }
}
```

- [ ] **Step 4: Подключить оба новых модуля в `src/app.module.ts`**

```ts
import { RolesModule } from './modules/roles/roles.module';
import { PermissionsModule } from './modules/permissions/permissions.module';
// ...
  imports: [
    ConfigModule.forRoot(),
    PrismaModule,
    UsersModule,
    AuthModule,
    PetsModule,
    RolesModule,
    PermissionsModule,
  ],
```

- [ ] **Step 5: Ручная проверка**

```bash
npm run start:dev
```

В другом терминале (токен — из ответа `POST /api/auth/login` или signup):

```bash
TOKEN=<ваш access_token>
curl -s -H "Authorization: Bearer $TOKEN" http://localhost:3000/api/permissions
```

Ожидаемо: JSON-массив из 7 прав. `POST` с телом `{"name":"bad name"}` → 400 от ValidationPipe.

- [ ] **Step 6: Commit**

```bash
git add src/modules/permissions src/app.module.ts
git commit -m "Permissions module CRUD"
```

---

### Task 4: Roles-модуль (роли + раздача прав)

**Files:**
- Modify: `src/modules/roles/dto/create-role.dto.ts`
- Delete: `src/modules/roles/dto/update-role.dto.ts`
- Create: `src/modules/roles/dto/set-role-permissions.dto.ts`
- Modify: `src/modules/roles/roles.service.ts`
- Modify: `src/modules/roles/roles.controller.ts`

Generic `PATCH /roles/:id` из заготовки заменяем на `PATCH /roles/:id/permissions` (спека).

- [ ] **Step 1: DTO**

`create-role.dto.ts`:

```ts
import { IsString, MinLength } from 'class-validator';

export class CreateRoleDto {
  @IsString()
  @MinLength(3)
  name: string;
}
```

`set-role-permissions.dto.ts`:

```ts
import { IsArray, IsInt } from 'class-validator';

export class SetRolePermissionsDto {
  @IsArray()
  @IsInt({ each: true })
  permissionIds: number[];
}
```

Файл `update-role.dto.ts` удалить.

- [ ] **Step 2: Сервис**

Ключевой метод — `setPermissions`: Prisma `set` по many-to-many полностью заменяет набор прав роли.

```ts
import { ConflictException, Injectable, NotFoundException } from '@nestjs/common';
import { CreateRoleDto } from './dto/create-role.dto';
import { PrismaService } from '../../libs/database/prisma.service';
import { Prisma } from '../../generated/prisma/client';

@Injectable()
export class RolesService {
  constructor(private prisma: PrismaService) {}

  async create(createRoleDto: CreateRoleDto) {
    try {
      return await this.prisma.role.create({ data: createRoleDto });
    } catch (e) {
      if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2002') {
        throw new ConflictException('Role with this name already exists');
      }
      throw e;
    }
  }

  findAll() {
    return this.prisma.role.findMany({ include: { permissions: true } });
  }

  async findOne(id: number) {
    const role = await this.prisma.role.findUnique({
      where: { id },
      include: { permissions: true },
    });
    if (!role) throw new NotFoundException(`Role with ID ${id} not found`);
    return role;
  }

  async setPermissions(id: number, permissionIds: number[]) {
    try {
      return await this.prisma.role.update({
        where: { id },
        data: {
          permissions: { set: permissionIds.map((pid) => ({ id: pid })) },
        },
        include: { permissions: true },
      });
    } catch (e) {
      if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2025') {
        throw new NotFoundException('Role or one of the permissions not found');
      }
      throw e;
    }
  }

  async remove(id: number) {
    try {
      return await this.prisma.role.delete({ where: { id } });
    } catch (e) {
      if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2025') {
        throw new NotFoundException(`Role with ID ${id} not found`);
      }
      throw e;
    }
  }
}
```

- [ ] **Step 3: Контроллер**

```ts
import { Controller, Get, Post, Body, Patch, Param, Delete, ParseIntPipe } from '@nestjs/common';
import { RolesService } from './roles.service';
import { CreateRoleDto } from './dto/create-role.dto';
import { SetRolePermissionsDto } from './dto/set-role-permissions.dto';

@Controller('roles')
export class RolesController {
  constructor(private readonly rolesService: RolesService) {}

  @Post()
  create(@Body() createRoleDto: CreateRoleDto) {
    return this.rolesService.create(createRoleDto);
  }

  @Get()
  findAll() {
    return this.rolesService.findAll();
  }

  @Get(':id')
  findOne(@Param('id', ParseIntPipe) id: number) {
    return this.rolesService.findOne(id);
  }

  @Patch(':id/permissions')
  setPermissions(
    @Param('id', ParseIntPipe) id: number,
    @Body() setRolePermissionsDto: SetRolePermissionsDto,
  ) {
    return this.rolesService.setPermissions(id, setRolePermissionsDto.permissionIds);
  }

  @Delete(':id')
  remove(@Param('id', ParseIntPipe) id: number) {
    return this.rolesService.remove(id);
  }
}
```

- [ ] **Step 4: Ручная проверка**

```bash
curl -s -H "Authorization: Bearer $TOKEN" http://localhost:3000/api/roles
```

Ожидаемо: `admin` и `user` с вложенными `permissions`. Затем `PATCH`:

```bash
curl -s -X PATCH -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"permissionIds":[1,2]}' http://localhost:3000/api/roles/2/permissions
```

Ожидаемо: роль возвращается ровно с двумя правами. Верните полный набор повторным `npx prisma db seed`.

- [ ] **Step 5: Commit**

```bash
git add src/modules/roles
git commit -m "Roles module with permissions assignment"
```

---

### Task 5: Декоратор @RequirePermissions и PermissionsGuard (TDD)

**Files:**
- Create: `src/shared/decorators/require-permissions.decorator.ts`
- Create: `src/shared/guards/permissions.guard.ts`
- Test: `src/shared/guards/permissions.guard.spec.ts`
- Modify: `src/app.module.ts`

- [ ] **Step 1: Декоратор**

Тот же паттерн, что ваш `@Public()`:

```ts
import { SetMetadata } from '@nestjs/common';

export const PERMISSIONS_KEY = 'required_permissions';
export const RequirePermissions = (...permissions: string[]) =>
  SetMetadata(PERMISSIONS_KEY, permissions);
```

- [ ] **Step 2: Написать падающий тест `permissions.guard.spec.ts`**

```ts
import { ExecutionContext, ForbiddenException } from '@nestjs/common';
import { PermissionsGuard } from './permissions.guard';

describe('PermissionsGuard', () => {
  const reflector = { getAllAndOverride: jest.fn() };
  const prisma = { user: { findUnique: jest.fn() } };
  let guard: PermissionsGuard;

  const mockContext = (user?: object) =>
    ({
      getHandler: () => ({}),
      getClass: () => ({}),
      switchToHttp: () => ({ getRequest: () => ({ user }) }),
    }) as unknown as ExecutionContext;

  beforeEach(() => {
    jest.clearAllMocks();
    guard = new PermissionsGuard(reflector as never, prisma as never);
  });

  it('passes when no permissions metadata', async () => {
    reflector.getAllAndOverride.mockReturnValue(undefined);
    await expect(guard.canActivate(mockContext({ id: 1 }))).resolves.toBe(true);
    expect(prisma.user.findUnique).not.toHaveBeenCalled();
  });

  it('passes when user has the required permission', async () => {
    reflector.getAllAndOverride.mockReturnValue(['pets:update']);
    prisma.user.findUnique.mockResolvedValue({
      id: 1,
      role: { permissions: [{ name: 'pets:update' }, { name: 'pets:read' }] },
    });
    await expect(guard.canActivate(mockContext({ id: 1 }))).resolves.toBe(true);
  });

  it('throws 403 when permission is missing', async () => {
    reflector.getAllAndOverride.mockReturnValue(['users:manage']);
    prisma.user.findUnique.mockResolvedValue({
      id: 1,
      role: { permissions: [{ name: 'pets:read' }] },
    });
    await expect(guard.canActivate(mockContext({ id: 1 }))).rejects.toThrow(ForbiddenException);
  });

  it('throws 403 when user has no role', async () => {
    reflector.getAllAndOverride.mockReturnValue(['pets:read']);
    prisma.user.findUnique.mockResolvedValue({ id: 1, role: null });
    await expect(guard.canActivate(mockContext({ id: 1 }))).rejects.toThrow(ForbiddenException);
  });

  it('attaches permissions to request user', async () => {
    reflector.getAllAndOverride.mockReturnValue(['pets:update']);
    const user: { id: number; permissions?: string[] } = { id: 1 };
    prisma.user.findUnique.mockResolvedValue({
      id: 1,
      role: { permissions: [{ name: 'pets:update' }] },
    });
    await guard.canActivate(mockContext(user));
    expect(user.permissions).toEqual(['pets:update']);
  });
});
```

- [ ] **Step 3: Убедиться, что тест падает**

```bash
npx jest src/shared/guards/permissions.guard.spec.ts
```

Ожидаемо: FAIL — `Cannot find module './permissions.guard'`.

- [ ] **Step 4: Реализовать guard**

```ts
import { CanActivate, ExecutionContext, ForbiddenException, Injectable } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { PERMISSIONS_KEY } from '../decorators/require-permissions.decorator';
import { PrismaService } from '../../libs/database/prisma.service';

@Injectable()
export class PermissionsGuard implements CanActivate {
  constructor(
    private reflector: Reflector,
    private prisma: PrismaService,
  ) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const required = this.reflector.getAllAndOverride<string[]>(PERMISSIONS_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (!required || required.length === 0) return true;

    const request = context.switchToHttp().getRequest();
    const user = request.user;
    if (!user) throw new ForbiddenException('Not authenticated');

    const dbUser = await this.prisma.user.findUnique({
      where: { id: user.id },
      include: { role: { include: { permissions: true } } },
    });

    const userPermissions = dbUser?.role?.permissions.map((p) => p.name) ?? [];
    user.permissions = userPermissions;

    const hasAll = required.every((p) => userPermissions.includes(p));
    if (!hasAll) throw new ForbiddenException('Insufficient permissions');

    return true;
  }
}
```

- [ ] **Step 5: Тесты зелёные**

```bash
npx jest src/shared/guards/permissions.guard.spec.ts
```

Ожидаемо: 5 passed.

- [ ] **Step 6: Зарегистрировать глобально в `src/app.module.ts`**

Глобальные guard'ы срабатывают в порядке регистрации: `JwtAuthGuard` из `AuthModule` (импорты инициализируются раньше) → потом этот. То есть `request.user` уже заполнен.

```ts
import { APP_GUARD } from '@nestjs/core';
import { PermissionsGuard } from './shared/guards/permissions.guard';

@Module({
  imports: [
    // ...как было
  ],
  providers: [
    {
      provide: APP_GUARD,
      useClass: PermissionsGuard,
    },
  ],
})
export class AppModule {}
```

- [ ] **Step 7: Commit**

```bash
git add src/shared src/app.module.ts
git commit -m "RequirePermissions decorator and PermissionsGuard"
```

---

### Task 6: Защитить permissions/roles эндпоинты, назначить первого админа

**Files:**
- Modify: `src/modules/permissions/permissions.controller.ts:1-8`
- Modify: `src/modules/roles/roles.controller.ts:1-9`

- [ ] **Step 1: Навесить право на оба контроллера (уровень класса)**

В обоих файлах добавить импорт и декоратор:

```ts
import { RequirePermissions } from '../../shared/decorators/require-permissions.decorator';

@RequirePermissions('users:manage')
@Controller('permissions')  // и соответственно @Controller('roles')
```

- [ ] **Step 2: Проверить, что доступ закрылся**

```bash
curl -s -H "Authorization: Bearer $TOKEN" http://localhost:3000/api/roles
```

Ожидаемо: `403 Forbidden` — у вашего пользователя ещё нет роли.

- [ ] **Step 3: Назначить себя админом (bootstrap-проблема)**

Первому админу право выдать через API нельзя — некому. Одноразово через Prisma Studio:

```bash
npx prisma studio
```

В таблице `users` у своего пользователя поставить `roleId` = id роли `admin` (посмотреть в таблице `roles`, обычно 1).

- [ ] **Step 4: Проверить доступ админа**

Тот же `curl` из Step 2 — теперь `200` со списком ролей (токен перевыпускать не нужно: права читаются из БД).

- [ ] **Step 5: Commit**

```bash
git add src/modules/permissions src/modules/roles
git commit -m "Protect roles and permissions endpoints"
```

---

### Task 7: Signup назначает роль user

**Files:**
- Modify: `src/modules/auth/auth.service.ts:33-43`

- [ ] **Step 1: Обновить `signUp`**

`UsersService.create` принимает `Prisma.UserCreateInput` — роль подключаем через `connect` по имени, менять `UsersService` не нужно. Если роли нет в БД (не прогнан сид) — Prisma кинет P2025, отдаём понятную 500:

```ts
import { ConflictException, Injectable, InternalServerErrorException } from '@nestjs/common';
// ...существующие импорты
import { Prisma } from '../../generated/prisma/client';

  async signUp(createUserDto: CreateUserDto) {
    const hashedPassword = await hash(createUserDto.password, 10);
    try {
      const user = await this.usersService.create({
        ...createUserDto,
        password: hashedPassword,
        role: { connect: { name: 'user' } },
      });
      const { password, ...payload } = user;
      return {
        access_token: this.jwtService.sign(payload),
      };
    } catch (e) {
      if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2025') {
        throw new InternalServerErrorException(
          'Default role "user" not found — run "npx prisma db seed"',
        );
      }
      throw e;
    }
  }
```

- [ ] **Step 2: Проверить**

```bash
curl -s -X POST -H "Content-Type: application/json" \
  -d '{"email":"newbie@test.com","name":"Newbie","password":"secret1"}' \
  http://localhost:3000/api/auth/signup
```

Ожидаемо: `access_token`. В Prisma Studio у `newbie@test.com` заполнен `roleId` роли `user`.

- [ ] **Step 3: Commit**

```bash
git add src/modules/auth/auth.service.ts
git commit -m "Assign user role on signup"
```

---

### Task 8: Pets — permissions на эндпоинтах и ownership в сервисе (TDD)

**Files:**
- Create: `src/shared/interfaces/auth-user.interface.ts`
- Modify: `src/modules/pets/pets.service.ts`
- Modify: `src/modules/pets/pets.controller.ts`
- Test: `src/modules/pets/pets.service.spec.ts`

- [ ] **Step 1: Тип пользователя с правами**

`PermissionsGuard` кладёт `permissions` в `request.user` — опишем это типом (`src/shared/interfaces/auth-user.interface.ts`):

```ts
export interface AuthUser {
  id: number;
  email: string;
  permissions: string[];
}
```

- [ ] **Step 2: Написать падающий тест `pets.service.spec.ts`**

```ts
import { ForbiddenException, NotFoundException } from '@nestjs/common';
import { PetsService } from './pets.service';

describe('PetsService ownership', () => {
  const prisma = {
    pet: {
      findUnique: jest.fn(),
      update: jest.fn(),
      delete: jest.fn(),
    },
  };
  let service: PetsService;

  const owner = { id: 1, email: 'owner@test.com', permissions: ['pets:update', 'pets:delete'] };
  const stranger = { id: 2, email: 'other@test.com', permissions: ['pets:update', 'pets:delete'] };
  const admin = { id: 3, email: 'admin@test.com', permissions: ['pets:update', 'pets:manage-any'] };
  const pet = { id: 10, name: 'Rex', ownerId: 1 };

  beforeEach(() => {
    jest.clearAllMocks();
    prisma.pet.findUnique.mockResolvedValue(pet);
    service = new PetsService(prisma as never);
  });

  it('owner can update own pet', async () => {
    prisma.pet.update.mockResolvedValue(pet);
    await expect(service.update(10, owner, { name: 'Rexie' })).resolves.toEqual(pet);
  });

  it('stranger cannot update someone else pet', async () => {
    await expect(service.update(10, stranger, { name: 'X' })).rejects.toThrow(ForbiddenException);
    expect(prisma.pet.update).not.toHaveBeenCalled();
  });

  it('admin with pets:manage-any can update any pet', async () => {
    prisma.pet.update.mockResolvedValue(pet);
    await expect(service.update(10, admin, { name: 'X' })).resolves.toEqual(pet);
  });

  it('stranger cannot remove someone else pet', async () => {
    await expect(service.remove(10, stranger)).rejects.toThrow(ForbiddenException);
  });

  it('update of missing pet throws 404', async () => {
    prisma.pet.findUnique.mockResolvedValue(null);
    await expect(service.update(99, owner, { name: 'X' })).rejects.toThrow(NotFoundException);
  });
});
```

- [ ] **Step 3: Убедиться, что тест падает**

```bash
npx jest src/modules/pets/pets.service.spec.ts
```

Ожидаемо: FAIL (сигнатуры `update`/`remove` ещё старые, заглушки возвращают строки).

- [ ] **Step 4: Реализовать сервис**

```ts
import { ForbiddenException, Injectable, NotFoundException } from '@nestjs/common';
import { UpdatePetDto } from './dto/update-pet.dto';
import { PrismaService } from '../../libs/database/prisma.service';
import { Pet, Prisma } from '../../generated/prisma/client';
import { AuthUser } from '../../shared/interfaces/auth-user.interface';

@Injectable()
export class PetsService {
  constructor(private prisma: PrismaService) {}

  create(ownerId: number, createPetDto: Prisma.PetCreateWithoutOwnerInput) {
    return this.prisma.pet.create({
      data: {
        ...createPetDto,
        ownerId,
      },
    });
  }

  findAll() {
    return this.prisma.pet.findMany();
  }

  async findOne(id: number) {
    const pet = await this.prisma.pet.findUnique({ where: { id } });
    if (!pet) throw new NotFoundException(`Pet with ID ${id} not found`);
    return pet;
  }

  async update(id: number, user: AuthUser, updatePetDto: UpdatePetDto) {
    const pet = await this.findOne(id);
    this.checkOwnership(pet, user);
    return this.prisma.pet.update({ where: { id }, data: updatePetDto });
  }

  async remove(id: number, user: AuthUser) {
    const pet = await this.findOne(id);
    this.checkOwnership(pet, user);
    return this.prisma.pet.delete({ where: { id } });
  }

  private checkOwnership(pet: Pet, user: AuthUser) {
    if (pet.ownerId !== user.id && !user.permissions.includes('pets:manage-any')) {
      throw new ForbiddenException('You can only manage your own pets');
    }
  }
}
```

- [ ] **Step 5: Тесты зелёные**

```bash
npx jest src/modules/pets/pets.service.spec.ts
```

Ожидаемо: 5 passed.

- [ ] **Step 6: Контроллер — permissions и передача user**

```ts
import { Controller, Get, Post, Body, Patch, Param, Delete, ParseIntPipe } from '@nestjs/common';
import { PetsService } from './pets.service';
import { CreatePetDto } from './dto/create-pet.dto';
import { UpdatePetDto } from './dto/update-pet.dto';
import { GetUser } from '../auth/decorators/get-user.decorator';
import { Public } from '../auth/decorators/public.decorator';
import { RequirePermissions } from '../../shared/decorators/require-permissions.decorator';
import { AuthUser } from '../../shared/interfaces/auth-user.interface';

@Controller('pets')
export class PetsController {
  constructor(private readonly petsService: PetsService) {}

  @RequirePermissions('pets:create')
  @Post()
  create(@Body() createPetDto: CreatePetDto, @GetUser() user: AuthUser) {
    return this.petsService.create(user.id, createPetDto);
  }

  @Public()
  @Get()
  findAll() {
    return this.petsService.findAll();
  }

  @RequirePermissions('pets:read')
  @Get(':id')
  findOne(@Param('id', ParseIntPipe) id: number) {
    return this.petsService.findOne(id);
  }

  @RequirePermissions('pets:update')
  @Patch(':id')
  update(
    @Param('id', ParseIntPipe) id: number,
    @Body() updatePetDto: UpdatePetDto,
    @GetUser() user: AuthUser,
  ) {
    return this.petsService.update(id, user, updatePetDto);
  }

  @RequirePermissions('pets:delete')
  @Delete(':id')
  remove(@Param('id', ParseIntPipe) id: number, @GetUser() user: AuthUser) {
    return this.petsService.remove(id, user);
  }
}
```

- [ ] **Step 7: Commit**

```bash
git add src/modules/pets src/shared/interfaces
git commit -m "Pets permissions and ownership checks"
```

---

### Task 9: Сквозная ручная проверка

- [ ] **Step 1: Полный прогон тестов и сборка**

```bash
npx jest && npx tsc --noEmit
```

Ожидаемо: все тесты passed, компиляция чистая.

- [ ] **Step 2: Сценарий двух пользователей**

С работающим `npm run start:dev`:

1. Signup двух пользователей → два токена `T1`, `T2`.
2. `T1` создаёт питомца: `POST /api/pets` `{"name":"Rex"}` → 201, запомнить `id`.
3. `T2` пытается `PATCH /api/pets/<id>` `{"name":"Hacked"}` → **403** (чужой).
4. `T1` делает то же → **200** (свой).
5. `T2` пытается `GET /api/roles` → **403** (нет `users:manage`).
6. Админским токеном (пользователь из Task 6) `PATCH /api/pets/<id>` → **200** (`pets:manage-any`).
7. Без токена `GET /api/pets` → **200** (`@Public`), `GET /api/pets/<id>` → **401**.

- [ ] **Step 3: Финальный коммит (если остались правки)**

```bash
git status
git add -A src docs
git commit -m "RBAC finished"
```

---

## Порядок выполнения

Task 1 → 2 — фундамент (БД). Task 3 → 4 — API управления. Task 5 — сердце RBAC. Task 6 → 7 → 8 — применение. Task 9 — проверка. Каждая задача оставляет проект в рабочем состоянии.
