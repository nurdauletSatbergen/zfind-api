# Потеряшки (Lost Mode) — план реализации

> Workflow: разработчик реализует задачи сам, по одной. После каждой задачи — ревью (Claude), затем коммит и переход к следующей. Без юнит-тестов (решение разработчика, как в notifications); проверка — сборка + ручной smoke в Task 9. Task 8 пропущен.

**Goal:** Владелец переводит питомца в режим LOST; любой человек по QR-коду открывает публичную карточку без регистрации и в один тап сообщает «видел питомца»; владелец получает realtime-уведомление и видит хронологию.

**Architecture:** Два статуса `HOME`/`LOST` на `Pet` + история эпизодов `LostEpisode` (инвариант: не более одного активного, транзакции). Единая модель `Sighting` с опциональными полями и правилом минимума. Публичные эндпоинты — отдельный контроллер `public/*` c `@Public()`; rate-limit через `@nestjs/throttler` только на приёме sightings. Уведомление владельцу — новый метод `NotificationsService.notifyUser()` (переиспользует gateway).

**Tech Stack:** NestJS 11, Prisma 7 (PostgreSQL), `@nestjs/throttler` (новая зависимость), существующие FilesService/StorageService (MinIO), Socket.IO gateway из notifications.

**Спека:** `docs/specs/2026-07-30-lost-pets-spec.md`
**Ветка:** `feature/lost-pets` (создать от `main`)

**Ключевые паттерны проекта, которые надо соблюдать:**
- Prisma-клиент: `import { Prisma } from '../../generated/prisma/client'`; enum-типы (`PetStatus`, `NotificationType`) реэкспортируются оттуда же.
- `JwtAuthGuard` глобальный — публичные маршруты помечать `@Public()`; юзер достаётся `@GetUser()` (`JwtPayload = { id, email }`).
- Никаких `new Service()` — только constructor injection.
- Владение проверяется по `pet.ownerId === user.id` (`404` нет питомца / `403` чужой), RBAC здесь не нужен.

**Принятые в плане решения (не отступать молча):**
- WS-уведомление о sighting шлётся владельцу **всегда**, без гейта `notificationsOn` — это критичное событие (в отличие от broadcast-объявлений).
- Ошибки файла фото (тип/размер) отдаём `422` — так уже работает `imageFilePipe` во всём проекте (спека говорит «≤10 МБ, image/*», код ошибки не фиксирует).
- `rewardAmount` в JSON-ответах сериализуется строкой (Prisma Decimal) — это норма.

---

### Task 0: Ветка

- [ ] `git checkout -b feature/lost-pets`

---

### Task 1: Зависимость, схема Prisma, миграция с backfill, ValidationPipe

Схема многофайловая: модели лежат в `prisma/models/*.prisma` (в `prisma.config.ts` указано `schema: "prisma"`), корневой `prisma/schema.prisma` содержит только generator/datasource — его не трогаем.

**Files:** `prisma/models/pet.prisma`, `prisma/models/notification.prisma`, `prisma/models/user.prisma`, `prisma/models/file.prisma`, `prisma/migrations/<generated>/migration.sql`, `src/main.ts`

- [ ] Установить зависимость:

```bash
npm install @nestjs/throttler
```

- [ ] В `prisma/models/pet.prisma` заменить модель `Pet` и добавить рядом enum и две модели (существующая `PetPhoto` остаётся без изменений):

```prisma
enum PetStatus {
  HOME
  LOST
}

model Pet {
  id      Int    @id @default(autoincrement())
  name    String
  owner   User   @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  ownerId Int
  photos  PetPhoto[]

  publicCode   String    @unique
  status       PetStatus @default(HOME)
  rewardAmount Decimal?  @db.Decimal(10, 2)

  lostEpisodes LostEpisode[]
  sightings    Sighting[]

  @@index([status])
  @@map("pets")
}

model LostEpisode {
  id      Int       @id @default(autoincrement())
  pet     Pet       @relation(fields: [petId], references: [id], onDelete: Cascade)
  petId   Int
  lostAt  DateTime  @default(now())
  foundAt DateTime?

  @@index([petId, lostAt])
  @@map("lost_episodes")
}

model Sighting {
  id            Int      @id @default(autoincrement())
  pet           Pet      @relation(fields: [petId], references: [id], onDelete: Cascade)
  petId         Int
  lat           Float?
  lng           Float?
  address       String?
  comment       String?
  reporterPhone String?
  photo         File?    @relation(fields: [photoFileId], references: [id], onDelete: SetNull)
  photoFileId   Int?     @unique
  createdAt     DateTime @default(now())

  @@index([petId, createdAt])
  @@map("sightings")
}
```

- [ ] В `prisma/models/notification.prisma` в enum `NotificationType` добавить значение (ничего не удаляя):

```prisma
enum NotificationType {
  ANNOUNCEMENT
  SIGHTING
}
```

- [ ] В модель `User` (`prisma/models/user.prisma`) добавить поле, в модель `File` (`prisma/models/file.prisma`) — обратную связь:

```prisma
  // User
  phone String?

  // File
  sighting Sighting?
```

- [ ] Сгенерировать миграцию БЕЗ применения (в БД уже есть питомцы, `publicCode NOT NULL` упадёт):

```bash
npx prisma migrate dev --name lost_pets --create-only
```

- [ ] В сгенерированном `migration.sql` найти строку добавления `publicCode` в `pets` (`ADD COLUMN "publicCode" TEXT NOT NULL`) и заменить весь блок ALTER по `pets` на добавление nullable-колонки + backfill + NOT NULL:

```sql
ALTER TABLE "pets" ADD COLUMN "publicCode" TEXT,
ADD COLUMN "rewardAmount" DECIMAL(10,2),
ADD COLUMN "status" "PetStatus" NOT NULL DEFAULT 'HOME';

-- backfill publicCode для существующих питомцев (формат XXX-XXX, A-Z0-9)
UPDATE "pets" SET "publicCode" = (
  SELECT substr(code, 1, 3) || '-' || substr(code, 4, 3) FROM (
    SELECT string_agg(
      substr('ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789', (floor(random() * 36))::int + 1, 1),
      ''
    ) AS code
    FROM generate_series(1, 6)
    WHERE "pets"."id" IS NOT NULL
  ) t
);

ALTER TABLE "pets" ALTER COLUMN "publicCode" SET NOT NULL;
```

(`WHERE "pets"."id" IS NOT NULL` — коррелирует подзапрос, чтобы код генерировался на каждую строку, а не один раз. Строки `CREATE UNIQUE INDEX "pets_publicCode_key"` и `CREATE INDEX "pets_status_idx"` prisma сгенерирует сама — они должны идти ПОСЛЕ этого блока; при астрономически маловероятной коллизии упадёт создание уникального индекса — просто перезапустить миграцию на чистой попытке.)

- [ ] Применить: `npx prisma migrate dev` (сгенерирует и клиент). Проверить: `npx prisma studio` или psql — у существующих питомцев заполнен `publicCode`.

- [ ] В `src/main.ts` включить трансформацию (нужно для `@Type(() => Number)` в multipart-DTO, где всё приходит строками):

```typescript
  app.useGlobalPipes(new ValidationPipe({
    whitelist: true,
    transform: true
  }))
```

- [ ] `npm run build` — без ошибок.
- [ ] Ревью → коммит: `Add lost-pets schema: pet status, episodes, sightings`

---

### Task 2: PetsService — генерация publicCode, открыть findOwnedPet

**Files:** Modify `src/modules/pets/pets.service.ts`

- [ ] Обновить импорты и добавить генератор кода (экспортируется — понадобится в тестах):

```typescript
import {
  ForbiddenException,
  Injectable,
  InternalServerErrorException,
  NotFoundException,
  UnprocessableEntityException,
} from '@nestjs/common';
import { randomInt } from 'node:crypto';
import { CreatePetDto } from './dto/create-pet.dto';

const CODE_ALPHABET = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
const CODE_GENERATION_ATTEMPTS = 10;

export function generatePublicCode(): string {
  let code = '';
  for (let i = 0; i < 6; i++) {
    code += CODE_ALPHABET[randomInt(CODE_ALPHABET.length)];
  }
  return `${code.slice(0, 3)}-${code.slice(3)}`;
}
```

- [ ] Заменить `create()` — тип DTO меняем с `Prisma.PetCreateWithoutOwnerInput` на `CreatePetDto` (чтобы клиент не мог прислать `publicCode`/`status` в data), коллизия кода ловится по `P2002` с повторной генерацией:

```typescript
  async create(ownerId: number, createPetDto: CreatePetDto) {
    for (let attempt = 0; attempt < CODE_GENERATION_ATTEMPTS; attempt++) {
      try {
        return await this.prisma.pet.create({
          data: {
            ...createPetDto,
            ownerId,
            publicCode: generatePublicCode(),
          },
        });
      } catch (e) {
        if (
          e instanceof Prisma.PrismaClientKnownRequestError &&
          e.code === 'P2002'
        ) {
          continue;
        }
        throw e;
      }
    }
    throw new InternalServerErrorException(
      'Failed to generate a unique public code',
    );
  }
```

- [ ] У `findOwnedPet` убрать модификатор `private` (метод понадобится `LostModeService` и `SightingsService`; сигнатура и тело не меняются):

```typescript
  async findOwnedPet(id: number, userId: number) {
```

- [ ] `npm run build` — без ошибок.
- [ ] Ревью → коммит: `Generate unique publicCode on pet creation`

---

### Task 3: NotificationsService.notifyUser + экспорт из модуля

**Files:**
- Modify: `src/modules/notifications/notifications.service.ts`
- Modify: `src/modules/notifications/notifications.module.ts`

- [ ] В `notifications.service.ts` дополнить импорт из generated-клиента типом `NotificationType`:

```typescript
import { NotificationType, Prisma } from '../../generated/prisma/client';
```

- [ ] Добавить метод (адресное уведомление одному пользователю; в отличие от `broadcast`, WS-эмит НЕ гейтится `notificationsOn` — sighting критичен):

```typescript
  async notifyUser(
    userId: number,
    input: {
      type: NotificationType;
      title: string;
      body: string;
      data?: Prisma.InputJsonValue;
    },
  ) {
    const notification = await this.prisma.notification.create({
      data: {
        userId,
        type: input.type,
        title: input.title,
        body: input.body,
        data: input.data,
      },
    });
    this.gateway.sendToUser(userId, {
      id: notification.id,
      type: notification.type,
      title: notification.title,
      body: notification.body,
      data: input.data ?? null,
      createdAt: notification.createdAt,
    });
    return notification;
  }
```

- [ ] В `notifications.module.ts` добавить экспорт:

```typescript
@Module({
  providers: [NotificationsGateway, NotificationsService],
  imports: [
    PrismaModule,
    JwtModule.register({ secret: jwtConstants.secret })
  ],
  controllers: [NotificationsController],
  exports: [NotificationsService]
})
```

- [ ] `npm run build` — без ошибок.
- [ ] Ревью → коммит: `Add NotificationsService.notifyUser and export service`

---

### Task 4: DTO + подготовка FilesService/imageFilePipe к анонимной загрузке

**Files:**
- Create: `src/modules/pets/dto/change-status.dto.ts`
- Create: `src/modules/pets/dto/create-sighting.dto.ts`
- Create: `src/modules/pets/dto/public-pet.dto.ts`
- Modify: `src/modules/files/files.service.ts`
- Modify: `src/shared/pipes/image-file.pipe.ts`

- [ ] `dto/change-status.dto.ts`:

```typescript
import { Type } from 'class-transformer';
import { IsIn, IsNumber, IsOptional, Min } from 'class-validator';

export class ChangeStatusDto {
  @IsIn(['HOME', 'LOST'])
  status: 'HOME' | 'LOST';

  @IsOptional()
  @Type(() => Number)
  @IsNumber({ maxDecimalPlaces: 2 })
  @Min(0)
  rewardAmount?: number;
}
```

- [ ] `dto/create-sighting.dto.ts` (multipart — числа приходят строками, поэтому `@Type(() => Number)`; правило «минимум содержимого» проверяется в сервисе, не здесь):

```typescript
import { Type } from 'class-transformer';
import {
  IsNumber,
  IsOptional,
  IsString,
  Max,
  MaxLength,
  Min,
} from 'class-validator';

export class CreateSightingDto {
  @IsOptional()
  @Type(() => Number)
  @IsNumber()
  @Min(-90)
  @Max(90)
  lat?: number;

  @IsOptional()
  @Type(() => Number)
  @IsNumber()
  @Min(-180)
  @Max(180)
  lng?: number;

  @IsOptional()
  @IsString()
  @MaxLength(300)
  address?: string;

  @IsOptional()
  @IsString()
  @MaxLength(500)
  comment?: string;

  @IsOptional()
  @IsString()
  @MaxLength(20)
  reporterPhone?: string;
}
```

- [ ] `dto/public-pet.dto.ts` — форма публичного ответа (никаких `ownerId`/email/внутренних id):

```typescript
export interface PublicPetDto {
  publicCode: string;
  name: string;
  status: 'HOME' | 'LOST';
  photos: string[];
  recentlyFound: boolean;
  // только при status === 'LOST':
  rewardAmount?: string | null;
  ownerPhone?: string | null;
  lostAt?: Date;
}
```

- [ ] В `src/modules/files/files.service.ts` расширить scope и разрешить анонимную загрузку (нашедший не авторизован; колонка `uploadedById` в БД уже nullable):

```typescript
export type FileScope = 'pets' | 'avatars' | 'sightings';
```

```typescript
  async uploadPublic(
    scope: FileScope,
    entityId: number,
    file: Express.Multer.File,
    uploadedById: number | null,
  ): Promise<File> {
```

(тело метода не меняется — `uploadedById: null` Prisma примет как есть; существующие вызовы передают `number` и продолжают работать.)

- [ ] В `src/shared/pipes/image-file.pipe.ts` добавить опцию «файл необязателен» (для sighting фото опционально; поведение существующих вызовов `imageFilePipe()` не меняется):

```typescript
import { HttpStatus, ParseFilePipeBuilder } from '@nestjs/common';

export const MAX_IMAGE_SIZE_BYTES = 10 * 1024 * 1024;

export const imageFilePipe = (options?: { optional?: boolean }) =>
  new ParseFilePipeBuilder()
    .addFileTypeValidator({ fileType: /^image\/(jpeg|png|webp)$/ })
    .addMaxSizeValidator({ maxSize: MAX_IMAGE_SIZE_BYTES })
    .build({
      errorHttpStatusCode: HttpStatus.UNPROCESSABLE_ENTITY,
      fileIsRequired: !options?.optional,
    });
```

- [ ] `npm run build` — без ошибок.
- [ ] Ревью → коммит: `Add lost-pets DTOs, sighting file scope and optional image pipe`

---

### Task 5: LostModeService — машина состояний, эпизоды, публичная карточка, stats

**Files:** Create `src/modules/pets/lost-mode.service.ts`

- [ ] Полный файл:

```typescript
import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../../libs/database/prisma.service';
import { FilesService } from '../files/files.service';
import { PetsService } from './pets.service';
import { ChangeStatusDto } from './dto/change-status.dto';
import { PublicPetDto } from './dto/public-pet.dto';

export const RECENTLY_FOUND_DAYS = 14;

@Injectable()
export class LostModeService {
  constructor(
    private prisma: PrismaService,
    private petsService: PetsService,
    private filesService: FilesService,
  ) {}

  // HOME → LOST: открыть эпизод (+ опц. reward); LOST → HOME: закрыть эпизод, обнулить reward.
  // Инвариант «≤1 активного эпизода» держится тем, что оба изменения идут одной транзакцией,
  // а вход в LOST возможен только из HOME (тот же статус → 400).
  async changeStatus(petId: number, userId: number, dto: ChangeStatusDto) {
    const pet = await this.petsService.findOwnedPet(petId, userId);

    if (pet.status === dto.status) {
      throw new BadRequestException(`Pet is already in status ${dto.status}`);
    }
    if (dto.status === 'HOME' && dto.rewardAmount != null) {
      throw new BadRequestException(
        'rewardAmount is only allowed when switching to LOST',
      );
    }

    if (dto.status === 'LOST') {
      const [updated] = await this.prisma.$transaction([
        this.prisma.pet.update({
          where: { id: petId },
          data: { status: 'LOST', rewardAmount: dto.rewardAmount ?? null },
        }),
        this.prisma.lostEpisode.create({ data: { petId } }),
      ]);
      return updated;
    }

    const [updated] = await this.prisma.$transaction([
      this.prisma.pet.update({
        where: { id: petId },
        data: { status: 'HOME', rewardAmount: null },
      }),
      this.prisma.lostEpisode.updateMany({
        where: { petId, foundAt: null },
        data: { foundAt: new Date() },
      }),
    ]);
    return updated;
  }

  async listEpisodes(petId: number, userId: number) {
    await this.petsService.findOwnedPet(petId, userId);
    return this.prisma.lostEpisode.findMany({
      where: { petId },
      orderBy: { lostAt: 'desc' },
    });
  }

  async publicCard(code: string): Promise<PublicPetDto> {
    const pet = await this.prisma.pet.findUnique({
      where: { publicCode: code },
      include: {
        owner: { select: { phone: true } },
        photos: { include: { file: true }, orderBy: { position: 'asc' } },
        lostEpisodes: { where: { foundAt: null }, take: 1 },
      },
    });
    if (!pet) throw new NotFoundException('Pet not found');

    const dto: PublicPetDto = {
      publicCode: pet.publicCode,
      name: pet.name,
      status: pet.status,
      photos: await Promise.all(
        pet.photos.map((photo) => this.filesService.url(photo.file)),
      ),
      recentlyFound: await this.hasRecentReunion(pet.id),
    };

    if (pet.status === 'LOST') {
      dto.rewardAmount = pet.rewardAmount?.toString() ?? null;
      dto.ownerPhone = pet.owner.phone ?? null;
      dto.lostAt = pet.lostEpisodes[0]?.lostAt;
    }
    return dto;
  }

  async stats() {
    const [reunions, searching] = await this.prisma.$transaction([
      this.prisma.lostEpisode.count({ where: { foundAt: { not: null } } }),
      this.prisma.pet.count({ where: { status: 'LOST' } }),
    ]);
    return { reunions, searching };
  }

  private async hasRecentReunion(petId: number): Promise<boolean> {
    const cutoff = new Date(Date.now() - RECENTLY_FOUND_DAYS * 86_400_000);
    const count = await this.prisma.lostEpisode.count({
      where: { petId, foundAt: { gte: cutoff } },
    });
    return count > 0;
  }
}
```

- [ ] `npm run build` — без ошибок (сервис ещё не зарегистрирован в модуле — это Task 7).
- [ ] Ревью → коммит: `Add LostModeService: status machine, episodes, public card, stats`

---

### Task 6: SightingsService — приём «видели питомца», выдача владельцу

**Files:** Create `src/modules/pets/sightings.service.ts`

- [ ] Полный файл:

```typescript
import {
  BadRequestException,
  ConflictException,
  Injectable,
  Logger,
  NotFoundException,
} from '@nestjs/common';
import { PrismaService } from '../../libs/database/prisma.service';
import { FilesService } from '../files/files.service';
import { NotificationsService } from '../notifications/notifications.service';
import { PetsService } from './pets.service';
import { CreateSightingDto } from './dto/create-sighting.dto';

@Injectable()
export class SightingsService {
  private readonly logger = new Logger(SightingsService.name);

  constructor(
    private prisma: PrismaService,
    private filesService: FilesService,
    private notificationsService: NotificationsService,
    private petsService: PetsService,
  ) {}

  async createPublic(
    code: string,
    dto: CreateSightingDto,
    photo?: Express.Multer.File,
  ) {
    const pet = await this.prisma.pet.findUnique({
      where: { publicCode: code },
      select: { id: true, name: true, status: true, ownerId: true },
    });
    if (!pet) throw new NotFoundException('Pet not found');
    if (pet.status !== 'LOST') {
      throw new ConflictException('Pet is not lost');
    }

    const hasLat = dto.lat != null;
    const hasLng = dto.lng != null;
    if (hasLat !== hasLng) {
      throw new BadRequestException('lat and lng must be provided together');
    }
    // правило минимума: гео | адрес | комментарий (фото/телефон недостаточны)
    if (!hasLat && !dto.address && !dto.comment) {
      throw new BadRequestException(
        'Provide at least coordinates, address or comment',
      );
    }

    // мягкая деградация: сбой S3 не роняет создание sighting
    let photoFileId: number | null = null;
    if (photo) {
      try {
        const file = await this.filesService.uploadPublic(
          'sightings',
          pet.id,
          photo,
          null,
        );
        photoFileId = file.id;
      } catch (e) {
        this.logger.warn(
          `Sighting photo upload failed for pet ${pet.id}: ${String(e)}`,
        );
      }
    }

    const sighting = await this.prisma.sighting.create({
      data: {
        petId: pet.id,
        lat: dto.lat,
        lng: dto.lng,
        address: dto.address,
        comment: dto.comment,
        reporterPhone: dto.reporterPhone,
        photoFileId,
      },
    });

    try {
      await this.notificationsService.notifyUser(pet.ownerId, {
        type: 'SIGHTING',
        title: `🐾 ${pet.name}: питомца видели!`,
        body: dto.address ?? dto.comment ?? 'Отмечена геолокация',
        data: {
          petId: pet.id,
          sightingId: sighting.id,
          lat: dto.lat ?? null,
          lng: dto.lng ?? null,
        },
      });
    } catch (e) {
      this.logger.warn(
        `Sighting notification failed for pet ${pet.id}: ${String(e)}`,
      );
    }

    return { id: sighting.id, createdAt: sighting.createdAt };
  }

  async listForOwner(petId: number, userId: number) {
    await this.petsService.findOwnedPet(petId, userId);
    const sightings = await this.prisma.sighting.findMany({
      where: { petId },
      orderBy: { createdAt: 'desc' },
      include: { photo: true },
    });
    return Promise.all(
      sightings.map(async ({ photo, photoFileId, ...rest }) => ({
        ...rest,
        photoUrl: photo ? await this.filesService.url(photo) : null,
      })),
    );
  }
}
```

- [ ] `npm run build` — без ошибок.
- [ ] Ревью → коммит: `Add SightingsService with owner notification and soft photo degradation`

---

### Task 7: Контроллеры, throttler, wiring модулей

**Files:**
- Create: `src/modules/pets/pets-public.controller.ts`
- Modify: `src/modules/pets/pets.controller.ts`
- Modify: `src/modules/pets/pets.module.ts`
- Modify: `src/app.module.ts`

- [ ] `pets-public.controller.ts` (все маршруты `@Public()`; `ThrottlerGuard` — ТОЛЬКО на приёме sightings; лимит берётся из конфига в AppModule ниже):

```typescript
import {
  Body,
  Controller,
  Get,
  Param,
  Post,
  UploadedFile,
  UseGuards,
  UseInterceptors,
} from '@nestjs/common';
import { FileInterceptor } from '@nestjs/platform-express';
import { ThrottlerGuard } from '@nestjs/throttler';
import { Public } from '../auth/decorators/public.decorator';
import { imageFilePipe } from '../../shared/pipes/image-file.pipe';
import { LostModeService } from './lost-mode.service';
import { SightingsService } from './sightings.service';
import { CreateSightingDto } from './dto/create-sighting.dto';

@Controller('public')
export class PetsPublicController {
  constructor(
    private readonly lostModeService: LostModeService,
    private readonly sightingsService: SightingsService,
  ) {}

  @Public()
  @Get('stats')
  stats() {
    return this.lostModeService.stats();
  }

  @Public()
  @Get('pets/:code')
  publicCard(@Param('code') code: string) {
    return this.lostModeService.publicCard(code);
  }

  @Public()
  @UseGuards(ThrottlerGuard)
  @Post('pets/:code/sightings')
  @UseInterceptors(FileInterceptor('photo'))
  createSighting(
    @Param('code') code: string,
    @Body() dto: CreateSightingDto,
    @UploadedFile(imageFilePipe({ optional: true }))
    photo?: Express.Multer.File,
  ) {
    return this.sightingsService.createPublic(code, dto, photo);
  }
}
```

- [ ] В `pets.controller.ts` добавить импорты и три owner-эндпоинта (после существующего `update`, до `remove` — порядок с `:id`-маршрутами не конфликтует, т.к. пути двухсегментные):

```typescript
import { LostModeService } from './lost-mode.service';
import { SightingsService } from './sightings.service';
import { ChangeStatusDto } from './dto/change-status.dto';
```

```typescript
  constructor(
    private readonly petsService: PetsService,
    private readonly lostModeService: LostModeService,
    private readonly sightingsService: SightingsService,
  ) {}
```

```typescript
  @Patch(':id/status')
  changeStatus(
    @Param('id', ParseIntPipe) id: number,
    @GetUser() user: JwtPayload,
    @Body() dto: ChangeStatusDto,
  ) {
    return this.lostModeService.changeStatus(id, user.id, dto);
  }

  @Get(':id/sightings')
  listSightings(
    @Param('id', ParseIntPipe) id: number,
    @GetUser() user: JwtPayload,
  ) {
    return this.sightingsService.listForOwner(id, user.id);
  }

  @Get(':id/lost-episodes')
  listLostEpisodes(
    @Param('id', ParseIntPipe) id: number,
    @GetUser() user: JwtPayload,
  ) {
    return this.lostModeService.listEpisodes(id, user.id);
  }
```

- [ ] `pets.module.ts` целиком:

```typescript
import { Module } from '@nestjs/common';
import { PetsService } from './pets.service';
import { PetsController } from './pets.controller';
import { PetsPublicController } from './pets-public.controller';
import { LostModeService } from './lost-mode.service';
import { SightingsService } from './sightings.service';
import { PrismaModule } from '../../libs/database/prisma.module';
import { UsersModule } from '../users/users.module';
import { FilesModule } from '../files/files.module';
import { NotificationsModule } from '../notifications/notifications.module';

@Module({
  controllers: [PetsController, PetsPublicController],
  providers: [PetsService, LostModeService, SightingsService],
  imports: [PrismaModule, UsersModule, FilesModule, NotificationsModule],
})
export class PetsModule {}
```

- [ ] В `src/app.module.ts` добавить конфиг throttler (5 запросов / 10 минут с одного IP; действует только там, где навешан `ThrottlerGuard` — т.е. на POST sightings):

```typescript
import { minutes, ThrottlerModule } from '@nestjs/throttler';
```

```typescript
    ThrottlerModule.forRoot([{ ttl: minutes(10), limit: 5 }]),
```

(Примечание: за реверс-прокси счётчик по IP потребует `trust proxy` — вне рамок, отметить при деплое.)

- [ ] Существующие `@Public() findAll/findOne` в pets теперь отдают и `publicCode`/`rewardAmount` всем подряд — по спеке §6 чинить не здесь; поставить TODO-комментарий над `findAll`:

```typescript
  // TODO: findAll/findOne публичные и отдают сущность целиком (publicCode, rewardAmount) —
  // сузить выдачу/закрыть авторизацией отдельной задачей
```

- [ ] `npm run build` — без ошибок.
- [ ] Ревью → коммит: `Add lost-pets endpoints: status, sightings, public card, stats`

---

### Task 8: Юнит-тесты (ПРОПУЩЕН — решение разработчика 2026-07-31; код ниже оставлен на случай, если тесты понадобятся позже)

**Files:**
- Create: `src/modules/pets/pets.service.spec.ts`
- Create: `src/modules/pets/lost-mode.service.spec.ts`
- Create: `src/modules/pets/sightings.service.spec.ts`

- [ ] `pets.service.spec.ts`:

```typescript
import { generatePublicCode } from './pets.service';

describe('generatePublicCode', () => {
  it('always matches XXX-XXX with A-Z0-9', () => {
    for (let i = 0; i < 200; i++) {
      expect(generatePublicCode()).toMatch(/^[A-Z0-9]{3}-[A-Z0-9]{3}$/);
    }
  });
});
```

- [ ] `lost-mode.service.spec.ts`:

```typescript
import { BadRequestException, ForbiddenException } from '@nestjs/common';
import { LostModeService } from './lost-mode.service';
import { PrismaService } from '../../libs/database/prisma.service';
import { PetsService } from './pets.service';
import { FilesService } from '../files/files.service';

describe('LostModeService.changeStatus', () => {
  let prisma: {
    $transaction: jest.Mock;
    pet: { update: jest.Mock };
    lostEpisode: { create: jest.Mock; updateMany: jest.Mock };
  };
  let petsService: { findOwnedPet: jest.Mock };
  let service: LostModeService;

  beforeEach(() => {
    prisma = {
      // мокаем контракт "массив промисов -> массив результатов"
      $transaction: jest.fn((ops: Promise<unknown>[]) => Promise.all(ops)),
      pet: { update: jest.fn().mockResolvedValue({ id: 1, status: 'LOST' }) },
      lostEpisode: {
        create: jest.fn().mockResolvedValue({ id: 10 }),
        updateMany: jest.fn().mockResolvedValue({ count: 1 }),
      },
    };
    petsService = {
      findOwnedPet: jest.fn().mockResolvedValue({ id: 1, ownerId: 7, status: 'HOME' }),
    };
    service = new LostModeService(
      prisma as unknown as PrismaService,
      petsService as unknown as PetsService,
      {} as FilesService,
    );
  });

  it('rejects transition to the same status', async () => {
    petsService.findOwnedPet.mockResolvedValue({ id: 1, status: 'HOME' });
    await expect(
      service.changeStatus(1, 7, { status: 'HOME' }),
    ).rejects.toThrow(BadRequestException);
  });

  it('rejects rewardAmount when switching to HOME', async () => {
    petsService.findOwnedPet.mockResolvedValue({ id: 1, status: 'LOST' });
    await expect(
      service.changeStatus(1, 7, { status: 'HOME', rewardAmount: 100 }),
    ).rejects.toThrow(BadRequestException);
  });

  it('HOME → LOST creates an episode and sets reward in one transaction', async () => {
    await service.changeStatus(1, 7, { status: 'LOST', rewardAmount: 500 });
    expect(prisma.$transaction).toHaveBeenCalledTimes(1);
    expect(prisma.pet.update).toHaveBeenCalledWith({
      where: { id: 1 },
      data: { status: 'LOST', rewardAmount: 500 },
    });
    expect(prisma.lostEpisode.create).toHaveBeenCalledWith({
      data: { petId: 1 },
    });
  });

  it('LOST → HOME closes the active episode and clears reward', async () => {
    petsService.findOwnedPet.mockResolvedValue({ id: 1, status: 'LOST' });
    await service.changeStatus(1, 7, { status: 'HOME' });
    expect(prisma.pet.update).toHaveBeenCalledWith({
      where: { id: 1 },
      data: { status: 'HOME', rewardAmount: null },
    });
    expect(prisma.lostEpisode.updateMany).toHaveBeenCalledWith({
      where: { petId: 1, foundAt: null },
      data: { foundAt: expect.any(Date) },
    });
  });

  it('propagates Forbidden from ownership check', async () => {
    petsService.findOwnedPet.mockRejectedValue(new ForbiddenException());
    await expect(
      service.changeStatus(1, 999, { status: 'LOST' }),
    ).rejects.toThrow(ForbiddenException);
  });
});
```

- [ ] `sightings.service.spec.ts`:

```typescript
import {
  BadRequestException,
  ConflictException,
  NotFoundException,
} from '@nestjs/common';
import { SightingsService } from './sightings.service';
import { PrismaService } from '../../libs/database/prisma.service';
import { FilesService } from '../files/files.service';
import { NotificationsService } from '../notifications/notifications.service';
import { PetsService } from './pets.service';

describe('SightingsService.createPublic', () => {
  let prisma: {
    pet: { findUnique: jest.Mock };
    sighting: { create: jest.Mock };
  };
  let filesService: { uploadPublic: jest.Mock };
  let notifications: { notifyUser: jest.Mock };
  let service: SightingsService;

  const lostPet = { id: 1, name: 'Барсик', status: 'LOST', ownerId: 7 };

  beforeEach(() => {
    prisma = {
      pet: { findUnique: jest.fn().mockResolvedValue(lostPet) },
      sighting: {
        create: jest
          .fn()
          .mockResolvedValue({ id: 42, createdAt: new Date() }),
      },
    };
    filesService = {
      uploadPublic: jest.fn().mockResolvedValue({ id: 100 }),
    };
    notifications = { notifyUser: jest.fn().mockResolvedValue({}) };
    service = new SightingsService(
      prisma as unknown as PrismaService,
      filesService as unknown as FilesService,
      notifications as unknown as NotificationsService,
      {} as PetsService,
    );
  });

  it('404 when code is unknown', async () => {
    prisma.pet.findUnique.mockResolvedValue(null);
    await expect(
      service.createPublic('XXX-XXX', { comment: 'видел' }),
    ).rejects.toThrow(NotFoundException);
  });

  it('409 when pet is not LOST', async () => {
    prisma.pet.findUnique.mockResolvedValue({ ...lostPet, status: 'HOME' });
    await expect(
      service.createPublic('ABC-123', { comment: 'видел' }),
    ).rejects.toThrow(ConflictException);
  });

  it('400 when only phone is provided (minimum-content rule)', async () => {
    await expect(
      service.createPublic('ABC-123', { reporterPhone: '+777' }),
    ).rejects.toThrow(BadRequestException);
  });

  it('400 when lat is provided without lng', async () => {
    await expect(
      service.createPublic('ABC-123', { lat: 43.2 }),
    ).rejects.toThrow(BadRequestException);
  });

  it('creates sighting from geo only and notifies owner with SIGHTING', async () => {
    await service.createPublic('ABC-123', { lat: 43.2, lng: 76.9 });
    expect(prisma.sighting.create).toHaveBeenCalled();
    expect(notifications.notifyUser).toHaveBeenCalledWith(
      7,
      expect.objectContaining({ type: 'SIGHTING' }),
    );
  });

  it('S3 failure does not fail creation — sighting saved without photo', async () => {
    filesService.uploadPublic.mockRejectedValue(new Error('S3 down'));
    const result = await service.createPublic(
      'ABC-123',
      { comment: 'у школы' },
      { originalname: 'x.jpg' } as Express.Multer.File,
    );
    expect(result.id).toBe(42);
    expect(prisma.sighting.create).toHaveBeenCalledWith(
      expect.objectContaining({
        data: expect.objectContaining({ photoFileId: null }),
      }),
    );
  });
});
```

- [ ] Прогнать: `npm test -- --testPathPattern=pets` — все зелёные.
- [ ] `npm run lint` — чистый.
- [ ] Ревью → коммит: `Add lost-pets unit tests`

---

### Task 9: Ручная проверка (smoke)

Предусловия: БД и MinIO запущены, `npm run start:dev`, есть пользователь с питомцем. `$TOKEN` — JWT владельца из `POST /api/auth/login`.

- [ ] Создание питомца возвращает `publicCode` формата `XXX-XXX` и `status: "HOME"`:

```bash
curl -s -X POST http://localhost:3000/api/pets \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"name":"Барсик"}'
```

- [ ] Перевод в LOST с вознаграждением (`$CODE` — publicCode из ответа выше, `$PET_ID` — id):

```bash
curl -s -X PATCH http://localhost:3000/api/pets/$PET_ID/status \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"status":"LOST","rewardAmount":5000}'
```

Expected: `status: "LOST"`, `rewardAmount: "5000"`. Повторно тот же запрос → `400`. Чужим токеном → `403`.

- [ ] Публичная карточка БЕЗ токена: `curl -s http://localhost:3000/api/public/pets/$CODE` → есть `rewardAmount`, `lostAt`, `ownerPhone` (null, если телефон не заполнен), НЕТ `ownerId`. Несуществующий код → `404`.

- [ ] Sighting «один тап» без токена:

```bash
curl -s -X POST http://localhost:3000/api/public/pets/$CODE/sightings \
  -F lat=43.238949 -F lng=76.889709
```

Expected: `{"id":...,"createdAt":...}`. Полная форма: `-F comment='видел у школы' -F reporterPhone='+77001234567' -F photo=@cat.jpg` → тоже ок.

- [ ] Правило минимума: только `-F reporterPhone='+777'` → `400`. Rate-limit: 6-й запрос за 10 минут → `429`.

- [ ] Владелец: `GET /api/pets/$PET_ID/sightings` с токеном → хронология с `photoUrl`; `GET /api/notifications` → уведомление `SIGHTING`; в WS-консоли (как в notifications-плане) прилетает событие `notification`.

- [ ] Возврат домой: `PATCH .../status` c `{"status":"HOME"}` → `rewardAmount: null`; публичная карточка теперь без reward/телефона и с `recentlyFound: true`; `GET /api/pets/$PET_ID/lost-episodes` → эпизод с заполненным `foundAt`; `GET /api/public/stats` → `{"reunions":1,"searching":0}`.

- [ ] Sighting по питомцу в HOME → `409`.

- [ ] Ревью → финальный коммит, если были правки по ходу smoke.

---

### Вне плана (осознанно)

- Каталог «красивых» кодов, эскроу вознаграждения, Swagger, e2e-тесты через supertest (юнит-покрытие ядра есть в Task 8; e2e можно добавить отдельной задачей), выставление `User.phone` наружу (в `UpdateUserDto` его нет — добавить `phone` в профиль пользователя отдельной мини-задачей, без него `ownerPhone` в публичной карточке будет `null`).
